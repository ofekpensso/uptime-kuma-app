# Upstream Source

This repository contains application source code based on Uptime Kuma.

Upstream repository:
https://github.com/louislam/uptime-kuma

Pinned upstream version:
2.3.2

The upstream source code is stored under the `app/` directory.

The upstream LICENSE file is kept under `app/LICENSE`.
